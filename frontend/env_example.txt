# Environment Configuration for Howdy Test Agent
# Copy this to .env and fill in your API keys

# Stagehand Configuration (LOCAL mode)
# For LOCAL environment, you only need OpenAI API key
OPENAI_API_KEY=your_openai_api_key

# Alternative: Use MODEL_API_KEY if you prefer
# MODEL_API_KEY=your_model_api_key

# Backend API Configuration
# Your Docker backend will run on port 443
# Stagehand will connect to: http://localhost:443/v1
BACKEND_PORT=443

# For LOCAL mode, Stagehand will use your local browser
# Make sure Playwright is installed: pip install playwright && playwright install
